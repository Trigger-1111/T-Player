// Template for serverDefaults.local.js (gitignored — never committed).
//
// Copy this file to `serverDefaults.local.js` in this same folder and fill in
// your own server's values. That file is the one actually imported by
// serverDefaults.js; this example only exists so the repo documents the
// shape without leaking real credentials.
export const DEFAULT_SERVER_URL = 'https://your-server.example.com';
export const DEFAULT_API_KEY = 'paste-your-backend/.env-API_KEY-here';
